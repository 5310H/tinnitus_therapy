{
  "name": "project-automation",
  "version": "1.0.0",
  "scripts": {
    "build": "make",
    "run": "./my_c_program",
    "clean": "rm -rf build/ bin/",
    "release": "git add . && git commit -m 'Release' && git push",
    "test": "python3 test_script.py"
  }
}